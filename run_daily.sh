#!/usr/bin/env bash
# ============================================================
#  每日更新：拉取当日行情 → 计算三市场情绪 → 刷新波段信号
#  （每天收盘后跑一次即可，会写日志到 data/logs/）
# ============================================================
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "$ROOT/scripts/daily_update.sh" "$@"
