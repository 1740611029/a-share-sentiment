# DESIGN.md — A股情绪指标系统

深色专业金融终端风格。设计令牌全部定义在 `sentisys/web/templates/base.html` 的 `:root` 中，
改样式时优先改令牌，不要在页面里写死颜色。

风格基因参考 Kraken（紫色主色 / 冷灰中性 / 发丝级描边），按暗色环境提亮并降饱和。

## 1. 视觉主题

- **调性**：专业、精密、克制、可信。像交易台终端，不像消费级 SaaS。
- **密度**：紧凑但透气。表格行高 8px 内边距，面板内边距 18/20px。
- **原则**：用描边而非阴影建立层级；用等宽数字对齐数据；装饰只服务信息。

## 2. 配色令牌

| 令牌 | 值 | 用途 |
|---|---|---|
| `--bg-0` | `#0B0D12` | 页面底色 |
| `--bg-1` | `#11141B` | 面板 / 卡片 |
| `--bg-2` | `#171B24` | 表头 / 悬浮态 |
| `--bg-3` | `#1E2330` | 次级按钮 / 轨道 |
| `--bg-inset` | `#0E1117` | 表格底 / 内嵌块 |
| `--line-1` | `#1E2330` | 默认分隔线 |
| `--line-2` | `#2A3140` | 强调描边 |
| `--line-3` | `#3A4356` | 弹窗描边 |
| `--tx-1` | `#E6E9F0` | 主文本 |
| `--tx-2` | `#9AA3B8` | 次文本 |
| `--tx-3` | `#6B7489` | 弱文本 / 微标签 |
| `--acc` | `#8B5CF6` | 交互主色（仅用于选中态、链接、强调） |
| `--up` | `#FF6161` | **红 = 上涨 / 过热** |
| `--down` | `#2BD98A` | **绿 = 下跌 / 恐慌** |
| `--warn` | `#FBBF24` | 数据滞后 / 样本不足告警 |

情绪状态色（`app.py` 的 `LEVEL_COLORS`，冷 → 热）：
`极度恐慌 #34D399` → `恐慌 #4ADE80` → `偏冷 #86EFAC` → `中性 #94A3B8` → `偏热 #FBBF24` → `过热 #FB923C` → `极度过热 #F87171`

图表曲线色（避开红绿语义）：A股 `#60A5FA` / 创业板 `#FB923C` / 科创板 `#A78BFA`；
信号标记：极端恐慌 ▲`#2BD98A`、极端过热 ▼`#FF6161`、波段底部 ◆`#FBBF24`、波段顶部 ◆`#C084FC`。

## 3. 排版

- `--font-sans`：系统中文字体栈（PingFang SC / HarmonyOS Sans SC / 微软雅黑）。
  **刻意不引入网络字体**——本项目为本地离线工具，CJK 字体动辄数 MB，不值得。
- `--font-mono`：JetBrains Mono → SF Mono → Cascadia Mono → Consolas。
  **所有数字一律走等宽 + `font-variant-numeric: tabular-nums`**，保证列对齐。

| 角色 | 规格 |
|---|---|
| 主指标数字 | 46px / 500 / `--font-mono` / letter-spacing -0.02em |
| 次级指标 | 34px / 500 |
| 面板标题 | 13.5px / 500 |
| 正文 / 表格 | 13px / 12.5px，行高 1.55 |
| 微标签 `.micro` | 10px / 大写 / letter-spacing .12em / `--tx-3` |

## 4. 组件

- **面板 `.panel`**：`--bg-1` 底 + `--line-1` 1px 描边 + 12px 圆角。
- **卡片 `.card`**：同面板，内部纵向 12px 间距。
- **指标块 `.stat`**：`--bg-inset` 底 + 8px 圆角，键 10.5px 灰、值 17px 等宽。
- **按钮 `.btn`**：6px 圆角；选中态 `.on` 用 `--acc-bg` + `--acc-hi` 文字 + 紫色描边。
- **标签 `.tag`**：`.ok` 绿 / `.no` 红 / `.wait` 灰 / `.acc` 紫，1px 半透明描边。
- **表格**：`thead th` 吸顶；宽表用 `.tbl-wrap.tbl-sticky` 吸附首列；
  双行表头用 `.grp-row`，第二行自动 `top:29px`；行状态 `.fail-row` 浅红底、`.hit-row` 浅绿底。
- **占比条 `.bar`**：细 4px 紫条 + 等宽数字。
- **弹窗 `#modal`**：680px 宽、`--line-3` 描边、深阴影，卡片块用 `.mcard`。

## 5. 布局

侧边栏 226px 固定（sticky），顶栏 sticky 并含**数据状态条**：
三市场分数芯片 + 数据日期 + 滞后天数告警（滞后 >1 天转琥珀色）。
内容区最大宽 1560px。

间距刻度：4 / 8 / 12 / 16 / 18 / 20 / 22 / 26 px。圆角：5 / 8 / 12 px。

## 6. 动效

只做状态变化，不做入场动画。缓动统一 `cubic-bezier(.22,1,.36,1)`（ease-out-quart），
时长 0.12–0.6s。禁止回弹 / 弹性缓动。进度条宽度过渡 0.5–0.6s。

## 7. 响应式

| 断点 | 行为 |
|---|---|
| > 1080px | 侧边栏竖排固定；顶栏单行 |
| ≤ 1080px | 侧边栏转为顶部横向导航条；卡片单列；图表高 260px |
| ≤ 640px | 指标块 2 列；顶栏状态芯片占满整行；面板内边距降至 14px |

## 8. 禁止事项

- 不用纯黑 `#000` / 纯白 `#fff` 作大面积底色。
- 不用紫蓝渐变、霓虹发光、彩色阴影（"泛 AI 审美"特征）。
- 不滥用毛玻璃——仅顶栏一处，用于滚动时的可读性。
- 不混用非令牌颜色；不在模板里写死色值（图表色除外，集中在 `history.html` 顶部常量区）。
- 红绿语义不可反转：红 = 涨/过热，绿 = 跌/恐慌。

## 9. 改样式时的检查清单

1. 新颜色先加到 `:root` 令牌，再引用。
2. 数字加 `.n` 或确认在 `tbody td` 内（已默认等宽）。
3. 宽表加 `.tbl-wrap.tbl-sticky`，中文列加 `.txt` 或 `.wrap`。
4. 改完跑一次全页面渲染自检（7 个路由应全部 200）。
